version: '{build}'
clone_depth: 3
init:
- cmd: >-
    set PATH=%PATH:C:\Program Files\Git\usr\bin;=%

    set PATH=C:\MinGW\bin;%PATH%
build_script:
- cmd: >-
    mkdir build

    cd build

    cmake -G "MinGW Makefiles" ..

    cmake --build . --target mbc_test
test: off
deploy: off
